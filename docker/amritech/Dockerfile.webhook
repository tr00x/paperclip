FROM node:22-slim

RUN apt-get update && apt-get install -y --no-install-recommends \
    ffmpeg curl \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app
COPY mcp-servers/telegram-webhook/index.js .
COPY mcp-servers/telegram-webhook/package.json .

EXPOSE 3088
CMD ["node", "index.js"]
